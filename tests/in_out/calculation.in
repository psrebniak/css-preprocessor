$container: 640;

.col-1-4 {
    width: (${container} / 4);
}
.col-2-4 {
    width: (2 * (${container} / 4));
}
.col-3-4 {
    width: (3 * (${container} / 4));
}
.col-4-4 {
    width: (4 * (${container} / 4));
}
$test: (5 * (${container} / 4));
.concatenation_test {
    test: (test + ${test});
}
